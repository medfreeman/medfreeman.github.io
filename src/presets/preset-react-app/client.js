import {
  renderApp,
  createApp,
  createContainer,
  query
} from "@phenomic/plugin-renderer-react/lib/client"

import BodyRenderer from "../../plugins/plugin-renderer-react/BodyRenderer"

export { renderApp, createApp, createContainer, query, BodyRenderer }
